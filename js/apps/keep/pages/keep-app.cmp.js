'use strict';

// import keepService from '../services/keep.service.js';

export default {
  name: 'keepApp',
  template: `
        <section class="keep-app">
            KEEP APP PAGE
        </section>
    `
};
